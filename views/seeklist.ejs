<% layout('layouts/boilerplate') %>

    <div>
        <form action="/seek?_method=PUT" method="POST">
            <button class="btn btn-danger m-2 ">Back</button>
        </form>
    </div>

    <ul>
        <h4>We found the following players living in your area who are seeking partners/players !</h4>
        <% for(let user of users){ %>
            <%if(user.username !=usname){%>
                <div class="card mb-3">
                    <div class="row">
                        <div class="col-md-8">
                            <div class="card-body">
                                <h5 class="card-title">

                                    Instagram Username : <%= user.username %>

                                </h5>

                            </div>
                        </div>

                    </div>
                </div>
                <%}%>
                    <%}%>
                        <h6>Good Luck ! <br> You can contact the players on Instagram</h6>
    </ul>